module.exports = {
  'wanna in her': function(assert, done) {
    assert.ok(true);
    assert.notOk(true);
    return done();
  },
  'wanna in her again': function(assert, done) {
    assert.ok(true);
    assert.notOk(true);
    return done();
  }
};